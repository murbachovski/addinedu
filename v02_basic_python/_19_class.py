# 클래스 : 제품의 설계도
# 객체 : 설계도로 만든 제품
# 속성 : 클래스 안의 변수
# 메서드 : 클래스 안의 함수
# 생성자 : 객체를 만들 때 실행되는 함수
# 인스턴스 : 메모리에 살아있는 객체
# 객체 > 인스턴스 == 인스턴스는 객체 안에 포함되어 있음

# 클래스 정의
# class 클래스이름:

# 생성자
# def __init__(self, name): 
                # 매개변수

# 메서드
# def 메서드이름(self):
        
# 객체 = 클래스이름()
# 객체.메서드()
    # . : ~의
